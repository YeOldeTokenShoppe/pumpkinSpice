'use client';

// This component is no longer needed since we've replaced the floating badge
// with the laurel wreath around the Clerk button
export default function ConditionalIllumin80() {
  // Return null as we don't need the floating badge anymore
  return null;
}